
in all playbooks that run after python.yml, set:



 ansible_python_interpreter: "{{ aws.instance.venv.dir.path }}/bin/python"
 
 #########################################################################################


mariadb.yml (DONE, test if it works)

remove:

    - name: Install PyMySQL
      become: true
      pip: name=PyMySQL
      
use:

 ansible_python_interpreter: "{{ aws.instance.venv.dir.path }}/bin/python"


*********************************************************************************

deploy-django.yml

remove:

    - name: Install psycopg2 for ansible to be able to create postgresql users
      become: true
      pip: name=psycopg2-binary
      
 ansible_python_interpreter: "{{ aws.instance.venv.dir.path }}/bin/python"   
   
*********************************************************************************   
      